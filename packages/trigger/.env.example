# Trigger Lambda Configuration
# This Lambda is invoked by EventBridge Scheduler to execute scheduled agent tasks

# =============================================================================
# AWS Configuration
# =============================================================================
# AWS Region (automatically set in Lambda, but can be specified for local testing)
AWS_REGION=ap-northeast-1

# =============================================================================
# Cognito Machine User Authentication
# =============================================================================
# Machine User credentials for server-to-server authentication
# These are used to obtain JWT tokens for invoking the Agent API

# Cognito User Pool ID
COGNITO_USER_POOL_ID=ap-northeast-1_xxxxxxxxx

# Machine User Client ID
COGNITO_CLIENT_ID=your-machine-user-client-id

# Machine User Client Secret
COGNITO_CLIENT_SECRET=your-machine-user-client-secret

# =============================================================================
# Agent API Configuration
# =============================================================================
# Agent API endpoint URL (deployed via CDK)
# Example: https://xxx.execute-api.ap-northeast-1.amazonaws.com/prod
AGENT_API_URL=https://your-agent-api-url.execute-api.ap-northeast-1.amazonaws.com/prod

# =============================================================================
# DynamoDB Configuration
# =============================================================================
# Triggers table name (for recording execution history)
# This table stores both trigger definitions and execution records
TRIGGERS_TABLE_NAME=your-triggers-table-name

# =============================================================================
# Notes
# =============================================================================
# 1. Lambda Environment:
#    - These values are automatically configured by CDK during deployment
#    - AWS_REGION is automatically available in Lambda runtime
#
# 2. Local Testing:
#    - Set these values manually for local development/testing
#    - Ensure you have proper AWS credentials configured
#
# 3. Machine User Setup:
#    - Create a Machine User in Cognito User Pool with Client Credentials flow
#    - Grant necessary permissions to invoke Agent API
#    - Store credentials securely (consider using Secrets Manager in production)
